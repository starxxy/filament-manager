# FilamentPro - 工业级耗材管理系统

FilamentPro 是一个轻量级、现代化的 3D 打印耗材管理系统。它可以帮助你轻松追踪每一卷耗材的使用情况、库存状态、价格以及购买渠道。

## ✨ 主要功能

- **可视化库存管理**：支持网格视图和列表视图，直观展示耗材颜色、余量和状态。
- **智能追踪**：记录耗材的品牌、材质、重量、剩余量以及开封时间。
- **数据安全**：内置数据库备份与还原功能，支持定期自动备份。
- **多端同步**：基于 Web 技术，支持 PC 和移动端访问，数据实时同步。
- **AI 辅助** (可选)：集成 AI 识别功能，简化录入流程。
- **系统升级**：支持 Web 界面直接上传更新包进行系统升级。

## 🆕 更新日志 (v1.0.3)

本次更新主要集中在系统稳定性和部署便捷性上：

- **[修复] 端口映射问题**：修正了容器内部端口监听，现在统一使用标准端口 `3000`。
- **[优化] Docker 部署体验**：优化了 `Dockerfile` 和启动脚本，部署更流畅。
- **[新增] 数据库自动维护**：增强了数据库连接的稳定性。

## 🚀 快速部署

### 推荐：使用 Docker Compose

创建一个 `docker-compose.yml` 文件：

```yaml
version: '3'
services:
  filament-manager:
    image: stary19/filament-manager:latest
    container_name: filament-manager
    ports:
      - "3000:3000"
    volumes:
      - ./database.db:/app/database.db
      - ./backups:/app/backups
    restart: unless-stopped
```

运行：
```bash
docker-compose up -d
```

### 使用 Docker CLI

```bash
docker run -d \
  --name filament-manager \
  --restart unless-stopped \
  -p 3000:3000 \
  -v $(pwd)/database.db:/app/database.db \
  -v $(pwd)/backups:/app/backups \
  stary19/filament-manager:latest
```

访问地址：`http://localhost:3000`
